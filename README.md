# Verb 100

Приложение для iPhone (PWA) по книге **«김재우의 기본 동사 100»** — 58 базовых
глаголов, 100 уроков, один урок в день.

Интерфейс русский с переключением на корейский. Английские фразы — из
оглавления книги, без изменений.

## Структура

```
app/                 само приложение (статика, без сборки)
  index.html
  app.css
  app.js             движок: уроки, тест, повторение, прогресс
  data.js            автогенерируется из content/
  images/            иллюстрации day-001.png … day-100.png
  icons/             иконки PWA
content/
  days.json          100 дней: глагол, оттенок, ключевое предложение, RU/KO
  lessons.json       развёрнутое содержание уроков
docs/
  VERB100_картинки_брифинг.xlsx   бриф на 100 иллюстраций
  image-brief.csv
  style-prompt.txt   единый стиль для всех картинок
tools/
  build-data.py      content/*.json  →  app/data.js
  build-artifact.py  app/            →  build/artifact.html
```

## Урок дня

Разбор значения → Model Examples → Small Talk → Further Studies → проверка.
Порядок повторяет книгу; проверка и интервальное повторение добавлены сверху.

Фразы возвращаются карточками через 1, 3, 7, 21 и 60 дней. Ошибка сбрасывает
интервал обратно на один день.

Пропущенный день не обнуляет прогресс: текущим становится первый непройденный
урок, а не урок «по календарю».

## Разработка

```sh
python3 tools/build-data.py      # пересобрать данные после правки content/
python3 -m http.server 8000 --directory app
```

Прогресс хранится в localStorage на устройстве, сервер не нужен.

## Установка на iPhone

Открыть адрес в Safari → «Поделиться» → «На экран „Домой“». После этого
приложение запускается в полноэкранном режиме и работает офлайн.

## Иллюстрации

Файлы кладутся в `app/images/` с именами `day-001.png` … `day-100.png`,
16:9, минимум 1200×675. Если файла нет, урок показывает заглушку.
